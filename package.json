{
  "name": "didigitalstudio-landing",
  "version": "1.0.0",
  "description": "DI Digital Studio — landing oficial",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "serve": "npx --yes serve@14 -p 3000 .",
    "vercel-build": "node scripts/build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
